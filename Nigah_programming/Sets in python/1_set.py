# Set is collection of unordered items .Sets are mutable.
# Each element in set must be unique and immutable.

null_set = set()
print(null_set)
print(type(null_set))

collection = {1,3,4,2,2,"hello","world","world"}
print(collection)
print(len(collection))
